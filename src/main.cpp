#include<string>

int main(int argc, char *argv[]) {
    std::string filename("positions.txt");
    if(argc == 1) {
        std::cout << "using default file: " << filename;
    } else {
        filename =
    }
}